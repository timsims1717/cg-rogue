use amethyst::core::ecs::System;

pub struct InterpolationSystem;

impl<'s> System<'s> for InterpolationSystem {
    type SystemData = (

    );

    fn run(&mut self, (

    ): Self::SystemData) {

    }
}